# 메모앱 개발가이드

## 프로젝트 개요

### 기본 정보
- **주제**: PWA 기반 오프라인 라이프 관리 애플리케이션 (메모 + 캘린더 + 가계부)
- **프로젝트명**: 메모라이프
- **배포 URL**: https://memo-jade-mu.vercel.app (v2 배포 후 갱신 예정)

> **2026-07-27 프로젝트 확장 안내**: 기존 "내 메모장"(메모 전용 앱, 1~5단계 완료·배포됨)에서 범위를 확장해, 사용자가 참고 자료로 제공한 `memolife.html`(캘린더/가계부 포함, 폰 프레임 UI) 디자인·기능을 기준으로 앱을 새로 구성함. 기존 "내 메모장"의 완료 기록은 `PROGRESS.md`와 아래 1~5단계에 히스토리로 남겨두고, 6단계부터 새 범위로 진행. 기존에 배포 환경에 저장돼 있던 테스트 메모 데이터는 사용자 확인 하에 새로 시작(마이그레이션 안 함).

### 목적
- 메모, 일정(캘린더), 가계부(수입/지출)를 한 앱에서 오프라인으로 기록/관리

### 타깃 사용자
- 메모, 일정, 지출을 여러 앱에 나눠 쓰지 않고 한 곳에서 간단히 관리하고 싶은 일반 사용자

### 톤 & 비주얼 컨셉
- `memolife.html` 기준: 폰 프레임 목업 스타일, 라운드가 큰 카드형 UI, 파스텔 톤 카테고리 색상(민트/라벤더/핑크/그린) + 무채색 기본 톤

### 하지 않을 것 (Non-goals) — 잠정, 진행하며 조정 가능
- 서버 동기화/로그인/협업 기능 (데이터는 localStorage 기반 로컬 저장만)
- 리치 텍스트 편집, 이미지/파일 첨부
- 체크리스트에 없는 기능 임의 추가
- 기존 "내 메모장"(v1) 테스트 데이터 마이그레이션 (새로 시작하기로 결정됨)

## 작업 규칙

### 1. 승인 원칙
- "시작" 또는 "go"라는 말이 나오기 전까지는 코드를 읽고 분석만 하고, 실제로 파일을 만들거나 수정하지 않을 것.
- 여기서 "단계"는 1~9단계 같은 큰 번호 순서가 아니라 **하나의 완결된 기능 단위**(예: 헤더, 슬라이더, 더보기 메뉴의 "내보내기" 기능 등)를 뜻함.
  - 기능 하나가 UI(예: 3단계)와 실제 동작 로직(예: 5단계)처럼 서로 다른 번호의 체크리스트에 걸쳐 있을 수 있음. 이런 경우 UI만 먼저 만들고 로직은 나중에 따로 만들지 않고, **기능이 실제로 동작하도록 관련 항목을 한 번에 묶어서** 진행하는 것을 기본으로 함 (재작업 비용을 줄이기 위함).
  - 다만 서로 다른 기능을 한 세션에 여러 개 묶어 진행하는 것은 여전히 금지 (절대 금지 사항 참고).
- 승인은 **세션 전체에 한 번이 아니라, 기능 단위마다 매번** 받을 것.
- 한 기능이 끝나면 자동으로 다음 기능으로 넘어가지 말고, 반드시 새로 "시작"/"go"를 기다릴 것.
- 작업을 시작하기 전, 다음 형식으로 계획을 먼저 설명할 것.
  > "이번 단계에서는 [무엇을] 위해 [어떤 파일들]을 [어떻게] 수정하겠습니다."
  - 하나의 기능이 여러 번호의 체크리스트 항목을 함께 완료시키는 경우, 계획 설명 시 어느 항목들이 함께 체크되는지 명시할 것.

### 2. 작업 기록 규칙 (이전 작업 기억하기)
- 새 세션을 시작할 때 먼저 `PROGRESS.md` 파일이 있는지 확인하고, 있으면 내용을 읽을 것.
- 가장 마지막에 기록된 작업 내용을 요약해서 사용자에게 보고할 것.
- "다음 작업 제안"이 적혀 있으면 함께 보고할 것.
- `PROGRESS.md`가 없으면 "이전 작업 기록이 없습니다"라고 알릴 것.
- `PROGRESS.md` 맨 아래에 기록을 추가할 것 (기존 기록은 지우지 말고 누적할 것).
- 작업 순서는 체크리스트를 확인하여 순서대로 진행하되, 번호 순서는 권장 순서일 뿐 절대 규칙은 아님 — 진행 중인 기능이 다른 번호의 항목과 연결되어 있다면 그 항목까지 함께 진행 가능 (위 "1. 승인 원칙" 참고)
- 진행 완료된 체크리스트에 체크 표시 할 것
- 작업 전 항상 계획을 먼저 설명하고 승인을 받을 것
- 파일 수정 시 변경 내용을 먼저 요약할 것
- 각 작업 완료 후 CLAUDE.md 체크리스트를 반드시 업데이트 할 것
- [x] 처리 시 완료 날짜를 괄호로 표기할 것. 예: - [x] 로그인 UI (2026-04-01)

### 3. 검증 규칙 (기능 구현 후 확인)
- 자동화 테스트(로컬 서버/헤드리스 브라우저 등, `localhost`·`file://` 기준)를 통과했다고 해서 실제 환경에서도 정상 동작한다고 단정하지 말 것.
- 기능 구현 완료 후 확인은 2단계로 나눠 진행할 것.
  1. Claude가 자동화 테스트로 1차 확인
  2. 사용자가 실제 기기/네트워크 환경(폰 브라우저, 필요 시 PC를 LAN IP로 접속 등)에서 2차 확인
- 2차 확인이 끝나기 전까지는 다음 기능 단계로 넘어가지 말 것.

### 4. 배포 규칙 (2026-07-27 추가)
- **기능 단계가 하나 완료될 때마다 커밋 + push까지 함께 진행할 것** (여러 단계를 로컬에만 쌓아두고 나중에 한 번에 올리지 말 것).
  - 배경: v2(메모라이프) 전환 중 6~9단계를 로컬에서만 진행하고 push를 미뤘다가, 사용자가 배포 사이트에서 "가계부 기능이 안 보여요"라고 리포트했을 때 원인을 서비스 워커 캐시로 잘못 추측한 적이 있음. 실제 원인은 단순히 push가 안 된 것이었음.
- 배포된 사이트에 대한 버그 리포트를 받으면, 코드 원인을 분석하기 전에 먼저 `git status`/`git log`로 **애초에 push·배포됐는지부터 확인**할 것.
## 절대 금지 사항
- 승인 없이 파일 삭제 금지
- 여러 기능을 한 번에 진행 금지 (단, 하나의 기능이 여러 번호의 체크리스트 항목에 걸쳐 있는 경우는 예외 — 위 "1. 승인 원칙" 참고)
- 체크리스트에 없는 기능 임의 추가 금지
- 라이브러리 / 프레임워크 임의 설치 금지

---

## 기술 스택
- Frontend: HTML5, CSS3, Vanilla JavaScript
- PWA: Service Worker, Web App Manifest
- Storage: localStorage
- Backup: JSON 파일 내보내기/가져오기
- 배포: Vercel
- 버전관리: GitHub

---

## 디자인 기획

> 아래 값은 `memolife.html` 참고 파일에서 그대로 가져온 디자인 시스템 (2026-07-27 결정: memolife.html 디자인으로 전면 교체)

### 컬러 시스템

#### Light Mode (기본)
- Background(바깥): #EDEDEB / Frame Background: #FAFAF9
- Card: #FFFFFF
- Ink(텍스트): #22262B / Ink-soft(보조 텍스트): #9599A2
- Border: #F0F0EE
- Accent(강조, 버튼 등): bg #22262B, ink #FFFFFF

#### Dark Mode
- Background(바깥): #0D0E10 / Frame Background: #17181B
- Card: #1F2124
- Ink: #EDEEEF / Ink-soft: #8B8E94
- Border: #2B2D31
- Accent: bg #ECECEC, ink #181A1D

#### 카테고리 색상 (Light → Dark)
- Mint(메모): bg #DFF3EA→#16281F, ink #2F8F68→#7FE0BC
- Lavender(캘린더): bg #E8E4FB→#221D3C, ink #6C5CE0→#BBADF7
- Pink(지출/가계부): bg #FDE3EC→#34202B, ink #D6488C→#F3A8C8
- Green(수입): bg #E5F5E1→#1B2A18, ink #4C9A3F→#9BDD8B

#### CSS 변수 선언 규칙
- `:root`에 라이트모드 변수 선언, `[data-theme="dark"]`에서 다크모드 변수 오버라이드
- 변수명: `--bg`, `--card`, `--ink`, `--ink-soft`, `--border`, `--accent-bg`, `--accent-ink`, `--mint-bg`/`--mint-ink` 등 memolife.html 네이밍 그대로 유지 (일관성 위해 임의 변경 금지)

### 타이포그래피
- 본문: 'Nanum Gothic', 'Inter', -apple-system, 'Apple SD Gothic Neo', sans-serif
- 브랜드 로고: 'Elms Sans'
- 숫자(금액 등): 'Bitcount Grid Double', 'Oswald', monospace 계열
- Google Fonts CDN 사용 (`fonts.googleapis.com`) — 오프라인 최초 접속 시 폰트는 시스템 폰트로 대체되고, 이후 접속부터는 브라우저 캐시로 로드됨

### 아이콘
- 별도 아이콘 라이브러리 없이 이모지 사용 (📝 캘린더 📅 가계부 💰 등, memolife.html 방식 그대로)

### UI 레이아웃 규칙
- 폰 프레임(`.frame`, 402×874px, 라운드 44px) 안에 전체 UI가 들어가는 목업 스타일
- 상단바(로고+날짜, 우측 다크모드 토글) → 스크린 영역(탭별 전환, `.screen.active`) → 하단 플로팅 탭바(메모/＋추가/캘린더/가계부)
- 화면 전환은 탭 클릭 시 `.screen`에 `active` 클래스 토글 (라우팅 없이 단일 페이지 내 전환)
- 삭제는 즉시 삭제가 아니라 토스트로 "실행취소" 제공하는 소프트 삭제 패턴

## 파일 구조

```
내메모장/
├── index.html          → 앱 엔트리 포인트
├── manifest.json        → PWA 매니페스트
├── service-worker.js    → 오프라인 캐싱
├── css/                  → 스타일시트
├── js/                   → 로직
└── icons/                → 앱 아이콘 (icon-192x192.png, icon-512x512.png)
```

---

## 코딩 규칙

### HTML
- 시맨틱 태그 우선 사용 (header, nav, main, article, section, footer)
- 주석은 한국어로 작성
- 들여쓰기는 2칸

### CSS
- 클래스명은 명확한 의미 전달
- Vanilla CSS 사용 (프레임워크 사용 금지)
- 주석으로 섹션 구분
- 컬러 시스템은 CSS 변수로 관리 (변수명 임의 변경 금지)

### JavaScript
- ES6+ 문법 사용
- 가독성을 위해 함수는 작게 분리
- 주석은 한국어로 작성

---

## 데이터 구조 (v2, memolife.html 기준)

> localStorage 키: `memolife_memos`, `memolife_schedules`, `memolife_transactions`, `memolife_theme`, `memolife_pin_order`, `memolife_sched_pin_order`, `memolife_tx_pin_order`, `memolife_budget`, `memolife_categories`

### 메모
```js
{
  id: 1,              // 고유 ID (Date.now())
  title: "메모 제목",
  body: "메모 내용",
  date: "2024-12-25",
  pinned: false        // 최대 개수 제한 있음 (MAX_PIN)
}
```

### 일정 (캘린더)
```js
{
  id: 1,
  title: "일정 제목",
  date: "2024-12-25",   // YYYY-MM-DD
  time: "14:00",         // 선택, 없으면 "시간 미정"
  repeat: "none",         // none | (반복 옵션)
  notify: false,          // 해당 시간에 브라우저 알림
  pinned: false
}
```

### 가계부 (거래 내역)
```js
{
  id: 1,
  type: "expense",      // expense | income
  amount: 10000,
  category: "식비",
  date: "2024-12-25",
  memo: "",
  pinned: false
}
```

## 전체 진행 상황

---

## 1단계. 프로젝트 기본 설정

### 파일 구조 생성
- [x] index.html, css/, js/, icons/ 폴더 생성 (2026-07-27)
- [x] manifest.json 작성 (2026-07-27)
- [x] service-worker.js 기본 골격 작성 (2026-07-27)

### Git 설정
- [x] Git 초기화 및 .gitignore 작성 (2026-07-27)
- [ ] GitHub 원격 저장소 연결 (사용자 GitHub 계정/저장소 정보 필요 — 별도 진행)

---

## 2단계. 메모앱 기본 기능 (CRUD)

### 메인화면 디자인 초안 잡기
- [x] 메모 카드 리스트 레이아웃 (2026-07-27)
- [x] 메모 추가 버튼(FAB) (2026-07-27, 1단계에서 완료)

### 배포하기 (초안 확인용)
- [x] Vercel 최초 배포 (2026-07-27) — https://memo-jade-mu.vercel.app

### 메모 저장/불러오기
- [x] localStorage 저장/불러오기 함수 (2026-07-27)

### CRUD 기능
- [x] 메모 추가하기 (2026-07-27)
- [x] 메모 수정하기 (2026-07-27)
- [x] 메모 삭제하기 (2026-07-27)
- [x] 메모 목록 보기 (2026-07-27)

### 검색 & 필터
- [x] 검색 기능 (2026-07-27)
- [x] 정렬 (최신순/오래된순) (2026-07-27)

---

## 3단계. UI 확장

- [x] 다크모드 (2026-07-27)
- [x] 클립보드 복사 (2026-07-27)
- [x] 공유 기능 (2026-07-27)
- [x] 태그/카테고리 (2026-07-27)
- [x] 즐겨찾기 (2026-07-27)

---

## 4단계. PWA 마무리

- [x] 오프라인 작동 검증 (2026-07-27, 실기기)
- [x] 홈 화면 설치 검증 (2026-07-27, 실기기)

---

## 5단계. 추가 선택 기능

- [x] 메모 개수 표시 (2026-07-27)
- [x] 중요 메모 고정 (2026-07-27)

---

# v2. 메모라이프 확장 (2026-07-27~)

> 아래부터는 `memolife.html` 참고 기준으로 범위를 확장한 새 단계. 위 1~5단계(v1 "내 메모장")는 완료된 상태로 유지하되, 화면/데이터 구조는 v2로 교체됨.

## 6단계. 디자인 시스템 + 앱 골격

- [x] 폰 프레임 레이아웃 (`.frame`) + 전역 컬러/폰트 변수 (`css/style.css` 재작성) (2026-07-27)
- [x] 상단바 (로고, 날짜, 다크모드 토글 버튼) (2026-07-27)
- [x] 하단 탭바 (홈 / 메모 / ＋추가 / 캘린더 / 가계부) (2026-07-27)
- [x] 화면 전환 로직 (`switchTab`, `.screen.active` 토글) (2026-07-27)
- [x] 토스트 + 실행취소 액션 지원 공통 함수 (`toast()`, 실제 소프트 삭제 적용은 7~9단계에서) (2026-07-27)

## 7단계. 메모 탭 (memolife 방식)

- [x] 메모 추가/수정/삭제 (소프트 삭제+실행취소) (2026-07-27)
- [x] 메모 고정(pin, 최대 3개 제한, 드래그로 순서 변경) (2026-07-27)
- [x] localStorage 저장/불러오기 (`memolife_memos`) (2026-07-27)
- [x] 메모 공유하기 (2026-07-27 추가 요청, `navigator.share` + 클립보드 복사 폴백)

## 8단계. 캘린더 탭

- [x] 월 달력 뷰 (`renderCalendar`) (2026-07-27)
- [x] 일정 추가/수정/삭제, 날짜 선택 (2026-07-27)
- [x] 반복 일정 옵션 (2026-07-27)
- [x] 시간 알림 (브라우저 Notification API) (2026-07-27)
- [x] 일정 고정(pin) (2026-07-27)

## 9단계. 가계부 탭

- [x] 수입/지출 기록 추가/수정/삭제 (2026-07-27)
- [x] 카테고리 관리 (추가/삭제, 사용 중인 카테고리 삭제 방지) (2026-07-27)
- [x] 이번 달 수입/지출 통계 (2026-07-27)
- [x] 예산 설정 및 초과 시 경고 (2026-07-27)
- [x] 거래 고정(pin) (2026-07-27)

## 10단계. 홈 대시보드 & 통합 검색

- [x] 오늘 요약 카드 (일정/메모/지출 건수) (2026-07-27)
- [x] 최근 항목 리스트 (오늘의 타임라인 + 이번 주 지출 비교 + 1년 전 오늘) (2026-07-27)
- [x] 통합 검색 (메모+일정+가계부, 필터 칩) (2026-07-27)
- [x] 빠른 이동 버튼 (+ 메모, + 일정, + 지출) (2026-07-27)
- [x] 고정 항목(메모/일정/가계부) 3열 카드 그리드 (2026-07-27, 추가 구현)
- [x] 상단 공유 버튼 (오늘 요약 공유) (2026-07-27, 추가 구현)

## 11단계. 설정 & 백업

- [x] 데이터 내보내기 (JSON 백업 파일 저장) (2026-07-27)
- [x] 데이터 가져오기 (백업 파일 복원) (2026-07-27)

## 12단계. PWA 재검증 & 배포

- [ ] manifest.json / service-worker.js 캐시 목록 갱신 (새 파일 구조 반영)
- [ ] 오프라인 작동 재검증
- [ ] 홈 화면 설치 재검증
- [ ] Vercel 재배포 및 실기기 최종 확인
