# 작업 기록

## 2026-07-27 — 1단계: 프로젝트 기본 설정

### 완료한 작업
- 브리핑(`메모장앱_브리핑.txt`)과 작업 가이드 초안(`클로드앰디_초안.txt`)의 빈칸을 사용자와 함께 확정
  - 앱 이름: 내 메모장 / Storage: localStorage / 추가 기능: 검색·정렬·태그·즐겨찾기
- 확정된 내용을 `CLAUDE.md`로 저장 (아이콘 라이브러리는 프로젝트가 Vanilla JS이므로 lucide-react → Lucide 정적 SVG로 수정)
- 프로젝트 폴더 구조 생성: `css/`, `js/`, `icons/`
- 기존 아이콘 파일(`icon-192x192.png`, `icon-512x512.png`)을 `icons/`로 이동
- `index.html`, `css/style.css`, `js/app.js` 기본 골격 작성 (CRUD 로직은 아직 없음, 2단계 예정)
- `manifest.json` 작성 (PWA 설치용)
- `service-worker.js` 작성 (핵심 파일 캐싱, 오프라인 지원 기본 골격)
- `git init`으로 로컬 저장소 초기화, `.gitignore` 작성 (커밋은 아직 안 함)

### 검증 상태
- 1차(Claude 자동 확인): 파일 구조 및 HTML/manifest/service-worker가 참조하는 경로가 실제 파일과 일치하는지 구조적으로 검증 완료. 단, 로컬에 Node/Python 등 실행 환경이 없어 브라우저 렌더링 자동 테스트는 수행하지 못함.
- 2차(사용자 실기기 확인): **아직 미완료** — 아래 "다음 작업 제안" 참고

### 다음 작업 제안
1. 사용자가 로컬 서버(예: VS Code Live Server 확장, 또는 `python -m http.server`가 되는 환경)로 `index.html`을 열어 화면이 정상적으로 뜨는지 확인
   - `file://`로 직접 열면 서비스 워커 등록은 실패할 수 있음(정상 — http(s) 오리진에서만 동작), 화면 표시 자체는 확인 가능
2. 2차 확인 완료 후 → 2단계 "메모앱 기본 기능(CRUD)" 진행 (메인화면 디자인 확정 → localStorage 저장/불러오기 → 추가/수정/삭제/목록 → 검색·정렬)

## 2026-07-27 — 2단계: 메모앱 기본 기능 (CRUD)

### 완료한 작업
- 1단계 화면을 Live Server로 확인 → 정상 렌더링 확인됨 (헤더/빈 상태 문구/FAB 버튼 모두 의도대로 표시)
- `index.html`에 메모 작성/수정 모달 마크업 추가 (제목 입력, 내용 textarea, 저장/취소/닫기 버튼)
- `css/style.css`에 메모 카드 스타일과 모달 스타일 추가
- `js/app.js`에 CRUD 로직 구현
  - `loadMemos`/`saveMemos`로 localStorage 저장·불러오기
  - `renderMemos`로 메모 카드 목록 렌더링 (최신순 정렬, `escapeHtml`로 XSS 방지)
  - `openModal`/`closeModal`로 작성·수정 모달 제어 (같은 모달을 재사용, `editingId`로 모드 구분)
  - `handleFormSubmit`으로 추가/수정 저장, `handleDelete`로 확인창(confirm) 후 삭제
  - 이벤트 위임(`handleMemoListClick`)으로 카드별 수정/삭제 버튼 처리
- `service-worker.js` 캐시 버전을 v1 → v2로 올려 변경된 파일이 캐시에 반영되도록 함

### 검증 상태
- 1차(Claude 자동 확인): `index.html`/`js/app.js`의 id·클래스 참조가 서로 일치하는지, 코드 구조(중괄호·템플릿 리터럴)에 문제가 없는지 정적으로 검토 완료. 로컬에 브라우저 자동화 도구가 없어 실제 클릭/입력 동작까지는 자동으로 확인하지 못함.
- 2차(사용자 실기기 확인): **완료** — Live Server로 추가/수정/삭제 및 새로고침 후 유지까지 정상 동작 확인됨

### 다음 작업 제안
- 검색 & 정렬 기능 진행

## 2026-07-27 — 2단계: 검색 & 정렬

### 완료한 작업
- `index.html`에 검색 입력창 + 정렬(최신순/오래된순) select 추가 (검색바)
- `css/style.css`에 검색바 스타일 추가
- `js/app.js`에 `filterMemos`/`sortMemos` 추가, `renderMemos`가 검색어·정렬 상태를 반영하도록 수정
  - 메모가 아예 없을 때와 검색 결과가 없을 때의 안내 문구를 구분
- `service-worker.js` 캐시 버전 v2 → v3

### 검증 상태
- 1차(Claude 자동 확인): id/class 참조 일치 및 코드 구조 정적 검토 완료. 실제 입력/클릭 동작은 자동으로 확인하지 못함.
- 2차(사용자 실기기 확인): **완료** — 검색/필터링, 정렬 모두 정상 동작 확인됨. 이로써 2단계 전체 완료.

### 다음 작업 제안
- 3단계 "UI 확장" 진행. 서로 다른 기능(다크모드/클립보드 복사/공유/태그/즐겨찾기)이므로 하나씩 순서대로 진행 예정. 우선 다크모드부터 제안.

## 2026-07-27 — 3단계: 다크모드

### 완료한 작업
- `index.html` 헤더에 다크모드 토글 버튼(🌙/☀️) 추가
- `css/style.css`에 헤더 flex 레이아웃 + 토글 버튼 스타일 추가 (기존에 준비돼 있던 `[data-theme="dark"]` 색상 변수를 실제로 사용)
- `js/app.js`에 다크모드 로직 추가
  - `getInitialTheme`: localStorage에 저장된 값이 있으면 그것을, 없으면 `prefers-color-scheme`로 시스템 설정을 따름
  - `applyTheme`: `<html>`에 `data-theme` 속성 설정 + 토글 버튼 아이콘/aria-label 갱신
  - 토글 클릭 시 localStorage에 저장해서 다음 방문에도 유지
- 코드 리뷰 중 순서 버그 발견 후 수정: `themeToggleBtn` DOM 참조 선언이 사용 위치보다 뒤에 있어 `ReferenceError`가 날 뻔한 것을 파일 상단으로 이동시켜 해결
- `service-worker.js` 캐시 버전 v3 → v4

### 검증 상태
- 1차(Claude 자동 확인): id 참조 일치, 변수 선언 순서(temporal dead zone 이슈) 재검토 완료.
- 2차(사용자 실기기 확인): **완료** — 다크모드 토글, 새로고침 후 유지 모두 정상 동작 확인됨

### 다음 작업 제안
- 3단계 다음 기능(클립보드 복사) 진행

## 2026-07-27 — 3단계: 클립보드 복사

### 완료한 작업
- 메모 카드마다 📋 복사 버튼 추가 (수정/삭제 버튼 옆)
- `js/app.js`에 `handleCopy`/`showCopyFeedback` 추가: `navigator.clipboard.writeText`로 "제목\n내용" 복사, 성공 시 ✅/실패 시 ⚠️로 1.5초간 아이콘 피드백 (그동안 버튼 비활성화로 중복 클릭 방지)
- `navigator.clipboard`를 지원하지 않는 환경 대비 처리 포함
- `css/style.css`에 `.icon-btn:disabled` 스타일 추가
- `service-worker.js` 캐시 버전 v4 → v5

### 검증 상태
- 1차(Claude 자동 확인): id/class 참조 일치, 코드 구조 정적 검토 완료.
- 2차(사용자 실기기 확인): **완료** — 복사 버튼 클릭 시 피드백 및 실제 클립보드 복사 정상 동작 확인됨

### 다음 작업 제안
- 3단계 다음 기능(공유 기능) 진행

## 2026-07-27 — 3단계: 공유 기능

### 완료한 작업
- 메모 카드에 📤 공유 버튼 추가 (복사 버튼 옆)
- `js/app.js`에 `handleShare` 추가: `navigator.share` 지원 시 기기 공유 시트 호출(제목+내용), 미지원 브라우저(대부분의 PC 브라우저)는 자동으로 `handleCopy`를 호출해 클립보드 복사로 대체
- 공유 취소 등으로 인한 Promise reject는 별도 알림 없이 무시 처리
- `service-worker.js` 캐시 버전 v5 → v6

### 검증 상태
- 1차(Claude 자동 확인): id/class 참조 일치, 코드 구조 정적 검토 완료.
- 2차(사용자 실기기 확인): **완료** — PC에서 클립보드 복사 대체 동작 확인됨. 이것으로 "추가 기능 3가지(다크모드/클립보드 복사/공유)" 필수 항목 모두 완료.

### 다음 작업 제안
- 3단계 다음 기능(태그/카테고리) 진행

## 2026-07-27 — 3단계: 태그/카테고리

### 완료한 작업
- 모달에 태그 입력창 추가 (쉼표로 구분해서 입력, 수정 시 기존 태그 값 채워짐)
- `js/app.js`: `handleFormSubmit`에서 태그 문자열을 배열로 파싱해 저장, `filterMemos`가 태그도 검색 대상에 포함하도록 수정
- 메모 카드에 태그를 `#태그` 형태의 칩으로 표시 (`css/style.css`에 `.memo-card-tags`/`.tag-chip` 스타일 추가)
- 태그 칩 클릭 시 검색창에 해당 태그가 채워지며 그 태그가 달린 메모만 필터링 (새 UI 없이 기존 검색 재사용)
- 보안: 태그 텍스트를 HTML 속성에 직접 넣지 않고 버튼의 표시 텍스트(`textContent`)로만 다루도록 해서 XSS 위험 회피
- `service-worker.js` 캐시 버전 v6 → v7

### 검증 상태
- 1차(Claude 자동 확인): id/class 참조 일치, 코드 구조 정적 검토 완료.
- 2차(사용자 실기기 확인): **완료** — 태그 저장/표시, 수정 시 값 유지, 태그 칩 클릭 필터링 모두 정상 동작 확인됨

### 다음 작업 제안
- 3단계 마지막 기능(즐겨찾기) 진행

## 2026-07-27 — 3단계: 즐겨찾기

### 완료한 작업
- 메모 카드 헤더 좌측에 ☆/★ 즐겨찾기 토글 버튼 추가 (제목 앞)
- `js/app.js`: `handleToggleFavorite`로 `isFavorite` 값을 토글하고 즉시 저장, `sortMemos`가 즐겨찾기를 항상 최상단에 먼저 배치하고 그 안에서 현재 정렬 기준(최신순/오래된순) 적용하도록 수정
- `css/style.css`: 헤더 레이아웃을 별 버튼(고정폭) + 제목(가변폭 `flex:1`) + 액션 버튼(고정폭) 3분할로 조정, 즐겨찾기 별 색상(주황) 스타일 추가
- 이것으로 3단계 "UI 확장" 체크리스트(다크모드/클립보드 복사/공유/태그/즐겨찾기) 전체 완료
- `service-worker.js` 캐시 버전 v7 → v8

### 검증 상태
- 1차(Claude 자동 확인): id/class 참조 및 이벤트 위임 분기 순서 정적 검토 완료.
- 2차(사용자 실기기 확인): **보류 (사용자가 나중에 하겠다고 함)** — ☆/★ 토글, 정렬 반영, 새로고침 후 유지 여부 아직 미확인. 다음 작업 시 리마인드 필요.

### 다음 작업 제안
- 배포 진행

## 2026-07-27 — 배포 (Vercel)

### 완료한 작업
- 로컬 git 첫 커밋 생성
- GitHub 저장소(`https://github.com/skdus4872-pixel/memo`)에 연결 및 push 완료
- 사용자가 Vercel에서 GitHub 저장소 Import → 배포 완료: **https://memo-jade-mu.vercel.app**
- `CLAUDE.md`에 배포 URL 기록, 체크리스트 "배포하기" 항목 체크

### 검증 상태
- 1차(Claude 자동 확인): 배포 URL로 직접 접속해 확인 완료
  - 메인 페이지 정상 로드 (제목/헤더/빈 상태 문구/정렬 옵션/모달 요소 모두 확인됨)
  - `manifest.json` 정상 서빙, name/icons/start_url 필드 확인됨
  - `service-worker.js` 정상 서빙, CACHE_NAME(v8)/install/fetch/activate 이벤트 확인됨
- 2차(사용자 실기기 확인): **아직 미완료**

### 다음 작업 제안
1. 사용자가 실제 폰에서 https://memo-jade-mu.vercel.app 접속해서 확인
   - 페이지가 정상적으로 뜨는지, 메모 CRUD가 실제 배포 환경에서도 잘 되는지
   - (미뤄뒀던) 즐겨찾기 ☆/★ 토글도 이 김에 같이 확인
   - 브라우저 메뉴에서 "홈 화면에 추가"가 뜨는지 (PWA 설치)
   - 공유 버튼(📤) 클릭 시 폰의 공유 시트가 실제로 뜨는지
   - 비행기 모드 등으로 네트워크를 끈 뒤 다시 접속해도 화면이 뜨는지 (오프라인 작동, 단 최초 1회는 온라인 상태로 접속해야 서비스워커가 설치됨)
2. 확인되면 → 4단계 "PWA 마무리" 체크리스트 완료 처리

## 2026-07-27 — 버그 수정: 공유 버튼 무반응

### 문제
- 배포 환경(PC 브라우저)에서 공유(📤) 버튼 클릭 시 아이콘 변화도, 클립보드 복사 대체도 없이 아무 반응이 없었음
- 원인 추정: 해당 브라우저에서 `navigator.share`가 존재는 하지만 호출이 동기적으로 예외를 던지거나 조용히 실패해서, 원래 의도했던 클립보드 복사 폴백까지 도달하지 못함 (기존 코드는 `.catch()`만 있고 동기 예외 대비가 없었음)

### 수정 내용
- `js/app.js`의 `handleShare`를 `try/catch`로 감싸서 동기 예외도 폴백 처리
- `navigator.share` 실패 시(`AbortError`로 사용자가 취소한 경우 제외) `handleCopy`로 폴백하도록 수정
- `service-worker.js` 캐시 버전 v8 → v9
- GitHub에 커밋/푸시 → Vercel 자동 재배포

### 검증 상태
- 1차(Claude 자동 확인): 코드 구조 검토 완료
- 2차(사용자 실기기 확인): **완료** — PC 브라우저에서 공유 버튼 클릭 시 정상 동작(클립보드 복사 대체) 확인됨

### 다음 작업 제안
- 여전히 미확인 상태로 남은 항목들: 즐겨찾기(☆/★) 실기기 테스트, 폰에서 PWA 설치("홈 화면에 추가"), 폰에서 공유 시트 실제 동작, 오프라인 작동 — 다음 세션에서 한 번에 확인 예정

## 2026-07-27 — 실기기(폰) 최종 확인

### 완료한 작업
- 사용자가 폰으로 https://memo-jade-mu.vercel.app 접속해서 아래 전부 확인함
  - 메모 CRUD 정상 동작
  - 즐겨찾기(☆/★) 토글 정상 동작 — 이전에 보류됐던 항목까지 확인 완료
  - PWA 홈 화면 설치 정상 동작
  - 공유 버튼으로 실제 공유 시트 정상 동작
  - 오프라인 상태에서도 화면 정상 표시
- `CLAUDE.md` 4단계 "PWA 마무리" 체크리스트(오프라인 작동/홈 화면 설치) 완료 처리

### 검증 상태
- 2차(사용자 실기기 확인): **완료** — 브리핑 필수 기능(CRUD, 오프라인, 홈 화면 설치, localStorage) + 추가 기능 3가지(다크모드/클립보드 복사/공유) + 선택 기능(검색/정렬/태그/즐겨찾기) 전부 실기기 검증 끝남

### 다음 작업 제안
- 브리핑 체크리스트 기준으로는 핵심 범위가 모두 완료됨. 추가로 원하는 게 없다면 여기서 마무리해도 되고, 남은 선택 항목(브리핑 5번의 "메모 개수 표시", "중요 메모 고정")을 더 진행할지는 사용자 의향에 따라 결정

## 2026-07-27 — 5단계: 메모 개수 표시

### 완료한 작업
- 검색/정렬 바 아래에 메모 개수 표시 영역 추가 (`index.html`)
- `js/app.js`에 `updateMemoCount` 추가, `renderMemos`의 모든 분기(빈 상태/검색 결과 없음/정상 렌더링)에서 호출되도록 연결
  - 검색 중이 아닐 때: "전체 N개"
  - 검색 중일 때: "검색 결과 M개 / 전체 N개"
- `css/style.css`에 `.memo-count` 스타일 추가
- `service-worker.js` 캐시 버전 v9 → v10
- `CLAUDE.md`에 "5단계. 추가 선택 기능" 섹션 신설 (메모 개수 표시 체크, 중요 메모 고정은 다음 논의 후 진행 예정)

### 검증 상태
- 1차(Claude 자동 확인): DOM 참조 순서 및 렌더링 분기별 개수 갱신 로직 정적 검토 완료.
- 2차(사용자 실기기 확인): **완료** — 개수 표시 정상 동작 확인됨

### 다음 작업 제안
- "중요 메모 고정" 진행 완료 (아래 참고)

## 2026-07-27 — 5단계: 중요 메모 고정

### 완료한 작업
- 사용자 결정: 즐겨찾기(★)와는 별개의 고정 기능으로 진행, 고정이 즐겨찾기보다 우선순위 높음
- 메모 카드에 📍/📌 고정 토글 버튼 추가 (즐겨찾기 별 왼쪽)
- `js/app.js`: `handleTogglePin`으로 `isPinned` 토글, `sortMemos`가 "고정 → 즐겨찾기 → 정렬 기준" 순으로 우선순위 적용하도록 수정, 새 메모 생성 시 `isPinned: false` 기본값 추가
- `css/style.css`에 `.pin-btn` 스타일 추가
- `service-worker.js` 캐시 버전 v10 → v11
- 이것으로 브리핑 5번 "추가하고 싶은 기능" 항목(검색/정렬/태그/즐겨찾기/메모개수/고정) 전부 완료

### 검증 상태
- 1차(Claude 자동 확인): id/class 참조, 정렬 우선순위 로직 정적 검토 완료.
- 2차(사용자 실기기 확인): **완료** — 고정/즐겨찾기 우선순위, 새로고침 후 유지 모두 정상 동작 확인됨

### 다음 작업 제안
- 커밋 및 배포 (아래 참고)

## 2026-07-27 — 최종 배포 확인 및 프로젝트 마무리

### 완료한 작업
- 메모 개수 표시 + 중요 메모 고정 기능을 커밋/푸시, Vercel 자동 재배포
- 사용자가 배포된 사이트(https://memo-jade-mu.vercel.app)에서 전체 기능 최종 확인 완료

### 검증 상태
- 2차(사용자 실기기 확인, 배포 환경): **완료** — 전체 기능 정상 동작 확인됨

### 프로젝트 현황
- 브리핑(`메모장앱_브리핑.txt`)에 명시된 모든 필수/선택 기능 구현 및 배포 완료
  - 메모 CRUD, localStorage 저장, PWA(오프라인 작동/홈 화면 설치)
  - 다크모드, 클립보드 복사, 공유 기능
  - 검색, 정렬, 태그, 즐겨찾기, 메모 개수 표시, 중요 메모 고정
- 배포 URL: https://memo-jade-mu.vercel.app
- GitHub: https://github.com/skdus4872-pixel/memo
- `CLAUDE.md` 체크리스트 전 항목 완료 처리됨

### 다음 작업 제안
- 특별히 남은 항목 없음. 추후 새 기능 요청이 있으면 여기서 이어서 진행

## 2026-07-27 — 프로젝트 확장: 메모라이프 (v2)로 전환

### 배경
- 사용자가 `files.zip`(참고 자료, 프로젝트 폴더 안에 직접 넣어둠)을 전달함. 내용물: `memolife.html`(캘린더+가계부까지 포함된 폰 프레임 스타일 목업, 1109줄), `manifest.json`, `sw.js`, 아이콘
- "저 파일 참고해서 다시 만들어줘" 요청에 대해 범위를 확인한 결과:
  - 캘린더/가계부까지 범위 확장하기로 결정
  - 디자인은 memolife.html 기준으로 전면 교체하기로 결정
  - 기존 배포 환경에 저장된 v1 테스트 메모 데이터는 마이그레이션하지 않고 새로 시작하기로 결정 (memolife.html이 어차피 다른 localStorage 키를 쓰기 때문에 충돌은 없음)

### 완료한 작업
- `CLAUDE.md`를 v2 범위로 업데이트
  - 프로젝트명 "내 메모장" → "메모라이프", 목적/타깃/톤 갱신
  - 디자인 기획 섹션을 memolife.html의 실제 컬러 변수·폰트로 교체
  - 데이터 구조에 일정(schedule)/가계부(transaction) 구조 추가
  - 6~12단계 체크리스트 신설 (디자인 골격 → 메모 탭 → 캘린더 탭 → 가계부 탭 → 홈 대시보드 → 설정/백업 → PWA 재검증/배포)
  - 기존 1~5단계(v1)는 완료 기록으로 그대로 유지
- `memolife.html`을 스크래치패드에 압축 해제해서 구조 파악 (홈/메모/캘린더/가계부/설정 화면, 토스트+실행취소, 알림, 백업 등 확인)

### 검증 상태
- 해당 없음 (문서/계획 작업)

### 다음 작업 제안
- 6단계 "디자인 시스템 + 앱 골격"부터 순서대로 진행 (사용자 "시작" 승인 대기 중)

## 2026-07-27 — 6단계: 디자인 시스템 + 앱 골격

### 완료한 작업
- `css/style.css` 전체 재작성: memolife.html의 컬러 변수(라이트/다크), 폰 프레임, 상단바, 화면 전환, 카드/라벨, 알약버튼, 목록 아이템, 폼, 캘린더 그리드, 가계부 통계, 탭바, 토스트, 고정 카드 그리드 스타일을 그대로 이식 (섹션별 한국어 주석 추가)
  - 참고 파일에는 없던 반응형 처리 추가: 뷰포트 440px 이하(실제 폰)에서는 `.frame`이 테두리 없이 화면 전체를 채우도록 미디어 쿼리 삽입 (원본은 데스크톱 목업 미리보기 전용 고정폭 402px이라, 실제 폰 배포 시 필요해서 추가)
- `index.html` 전체 교체: 폰 프레임(`#frame`) 안에 상단바(로고+날짜+다크모드 토글) → 화면 4개(홈/메모/캘린더/가계부, 지금은 자리표시자만) → 하단 탭바(홈/메모/＋/캘린더/가계부) → 토스트 구조로 재작성. Google Fonts(Nanum Gothic/Elms Sans/Oswald/Bitcount Grid Double/Inter) CDN 연결
- `js/app.js`에 골격 로직 작성
  - localStorage 공용 `load`/`save` 함수 및 `K`(키 모음) 객체
  - 오늘 날짜 라벨 표시
  - 탭 전환(`switchTab`), 가운데 ＋ 버튼은 우선 메모 탭으로 이동만 (폼 자동 오픈은 7단계에서 연결)
  - 다크모드 토글: memolife.html과 동일하게 `<html>`이 아니라 `#frame` 엘리먼트에 `data-theme` 속성을 적용 (원본 CSS 변수 상속 구조와 일치시키기 위함)
  - 토스트 공용 함수(`toast(message, actionLabel, onAction)`) — 실행취소 버튼 지원, 실제 삭제 로직 연결은 7~9단계에서
- 메모/캘린더/가계부 화면은 아직 "OO단계에서 만들 예정" 안내 문구만 있는 자리표시자 상태 (다음 단계들에서 채움)
- v1의 메모 CRUD 코드(`STORAGE_KEY`, CRUD 함수 등)는 이번 교체로 제거됨 — v1 완료 기록은 위 1~5단계와 앞선 PROGRESS 기록에 남아있음
- `service-worker.js`/`manifest.json`은 아직 미수정 (12단계에서 한 번에 정리 예정 — 그 전까지는 캐시된 이전 버전이 배포 사이트에 남아있을 수 있음)

### 검증 상태
- 1차(Claude 자동 확인): `index.html`의 id(frame/todayDate/themeBtn/screen-*/tabAddBtn/toast)와 `js/app.js`의 DOM 참조가 전부 일치하는지 확인. `.tab`/`.screen` 셀렉터 개수(4개씩)와 `data-tab`/`screen-` id 접미사 매칭 확인.
- 2차(사용자 실기기 확인): **완료** — 탭 전환, 다크모드, 로고/날짜 표시 정상 동작 확인됨

### 다음 작업 제안
- 7단계 "메모 탭" 진행

## 2026-07-27 — 7단계: 메모 탭

### 완료한 작업
- `index.html`의 메모 화면 자리표시자를 실제 마크업으로 교체 (작성 토글 버튼, 제목/내용 입력 폼, 메모 목록 카드)
- `js/app.js`에 공용 유틸 추가: `escapeHtml`(정규식 기반, 따옴표까지 이스케이프), `softDelete`(즉시 삭제 대신 토스트 "실행취소" 제공), `togglePin`(최대 개수 제한 있는 범용 고정 토글 — 8·9단계 캘린더/가계부에서도 재사용 예정)
- 메모 전용 로직: `renderMemos`(고정 메모 먼저, 나머지는 최신순), `toggleMemoPin`, 작성/수정 폼 저장(`memoSave`), 폼 닫힐 때 자동 초기화(`resetMemoForm`)
- 고정된 메모끼리 드래그 앤 드롭으로 순서 변경 가능 (memolife.html 원본 기능 그대로 이식)
- 탭바 가운데 ＋ 버튼이 메모 탭 이동 + 작성 폼 자동 오픈까지 연결됨
- `renderAll()`을 만들어서 현재는 `renderMemos()`만 호출 — 8~10단계에서 캘린더/가계부/홈 렌더 함수가 추가되면 여기에 이어붙일 예정

### 검증 상태
- 1차(Claude 자동 확인): `index.html`의 id(memoToggleBtn/memoForm/memoTitle/memoBody/memoSave/memoList)와 `js/app.js`의 DOM 참조가 전부 일치하는지 확인. 코드 구조(함수 선언 순서, `renderAll` 호출 시점) 정적 검토 완료.
- 2차(사용자 실기기 확인): **완료** — 작성/수정/삭제/실행취소/고정/드래그 정상 동작 확인됨

### 다음 작업 제안
- 8단계 "캘린더 탭" 진행

## 2026-07-27 — 8단계: 캘린더 탭

### 완료한 작업
- `index.html`의 캘린더 화면 자리표시자를 실제 마크업으로 교체 (월 이동 버튼, 달력 그리드, 일정 폼(반복/알림 옵션 포함), 일정 목록)
- `js/app.js`에 캘린더 로직 추가
  - `renderCalendar`: 월 그리드 렌더링, 일정 있는 날짜 표시(`has-item`), 오늘/선택 날짜 강조
  - 날짜 클릭 시 `picked` 갱신 → 해당 날짜 일정 목록(`renderCalList`) 갱신
  - 일정 추가 시 반복 옵션(매일 30일/매주 12주/매월 6개월)에 따라 여러 개 한 번에 생성, 수정 시엔 반복 옵션 비활성화
  - 알림 체크 시 브라우저 Notification 권한 요청, `checkScheduleAlerts`가 20초 간격으로 시간 도달 여부 확인해 알림(또는 권한 없으면 토스트)으로 표시
  - 고정(`toggleSchedPin`, 최대 3개)과 소프트 삭제는 메모 탭에서 만든 공용 `togglePin`/`softDelete` 함수 재사용
  - `renderAll()`에 `renderCalendar()` 추가

### 검증 상태
- 1차(Claude 자동 확인): `index.html`의 캘린더 관련 id(calPrevBtn/calNextBtn/calMonthLabel/calGrid/calDayLabel/calToggleBtn/calForm/calTitle/calTime/calRepeat/calNotify/calSave/calList)와 `js/app.js` DOM 참조 전부 일치 확인. `renderAll` 호출 순서, 함수 선언 위치 정적 검토 완료.
- 2차(사용자 실기기 확인): **완료** — 월 이동/날짜 선택, 일정 CRUD, 반복 등록, 알림 권한, 고정/드래그 모두 정상 동작 확인됨

### 다음 작업 제안
- 9단계 "가계부 탭" 진행

## 2026-07-27 — 9단계: 가계부 탭

### 완료한 작업
- `index.html`의 가계부 화면 자리표시자를 실제 마크업으로 교체 (이번 달 수입/지출 통계, 예산 설정 카드, 내역 작성 폼, 카테고리 편집 폼, 카테고리별 지출 막대그래프, 전체 내역 목록)
- `js/app.js`에 가계부 로직 추가
  - 카테고리: 기본 6종(식비/교통/쇼핑/생활/취미/기타), 추가/삭제 가능(사용 중이거나 마지막 1개는 삭제 불가)
  - 예산: 이번 달 예산 설정, 지출 대비 진행률 바 + 초과 시 빨간색 경고 표시
  - 이번 달 수입/지출 합계, 카테고리별 지출 막대그래프(내림차순)
  - `renderBudget`: 고정 거래 먼저, 나머지는 최신순 (메모/일정과 동일 패턴)
  - 거래 추가/수정/삭제(소프트 삭제+실행취소), 고정(`toggleTxPin`, 최대 3개, 드래그 순서 변경) — 전부 공용 `togglePin`/`softDelete` 재사용
  - 지출 등록 시 이번 달 예산 초과하면 "⚠ 이번 달 예산을 초과했어요" 토스트로 즉시 안내
  - `renderAll()`에 `renderBudget()` 추가 — 이제 메모/캘린더/가계부 3개 탭 모두 연결됨

### 검증 상태
- 1차(Claude 자동 확인): `index.html`의 가계부 관련 id 전부(`statIn`/`statOut`/`budgetEditBtn`/`budgetSetForm`/`budgetInput`/`budgetSaveBtn`/`budgetDisplay`/`txToggleBtn`/`txForm`/`txType`/`txAmount`/`txCategory`/`txMemo`/`txSave`/`catEditBtn`/`catEditForm`/`catEditList`/`catNewInput`/`catAddBtn`/`catBars`/`txList`)와 `js/app.js` 참조 일치 확인. `js/app.js` 전체 중괄호/소괄호/백틱 짝 맞음 확인(node 미설치로 실제 실행 검증은 못 함).
- 2차(사용자 실기기 확인): **아직 미완료**

### 다음 작업 제안
1. Live Server(Ctrl+Shift+R)로 열어서 확인
   - 가계부 탭에서 지출/수입 내역 추가 → 통계(수입/지출 합계)와 카테고리 막대그래프에 반영되는지
   - 예산 설정 후 지출 추가 시 진행률 바가 채워지는지, 예산 초과하면 경고 토스트/빨간 막대가 뜨는지
   - 카테고리 편집에서 추가/삭제 (사용 중인 카테고리는 삭제 안 되는 것도 확인)
   - 내역 수정/삭제(실행취소), 고정(최대 3개, 드래그 순서 변경)
2. 확인되면 → 10단계 "홈 대시보드 & 통합 검색" 진행

## 2026-07-27 — 버그 리포트: "가계부 기능이 안 보여"

### 원인
- 사용자가 배포 사이트(memo-jade-mu.vercel.app)에서 가계부 화면이 안 보인다고 리포트
- 1차 진단(성급했음): 서비스 워커 캐시가 v1으로 고정돼 있어서 그런가 의심 → `service-worker.js`의 `CACHE_NAME`을 `memo-app-cache-v11` → `memolife-cache-v1`로 변경
- **진짜 원인 확인**: `git status` 확인 결과 6~9단계(메모라이프 전체 개편) 작업이 로컬에서만 진행됐고 **한 번도 커밋/푸시된 적이 없었음** — 배포 사이트는 계속 v1 "내 메모장" 그대로였음. 서비스 워커 캐시 진단은 근거 없이 앞서나간 추측이었고, 사용자에게 정정 안내함.

### 완료한 작업
- 서비스 워커 캐시 이름 갱신(위 내용, 결과적으로는 유효한 방어 조치)
- 6~9단계 전체 변경사항(`CLAUDE.md`, `PROGRESS.md`, `css/style.css`, `index.html`, `js/app.js`, `service-worker.js`) 커밋 및 GitHub push → Vercel 자동 재배포 트리거

### 교훈 / 앞으로 지킬 것
- 배포 환경 버그 리포트를 받으면 **코드 분석보다 먼저 `git status`/`git log`로 "애초에 배포됐는지"부터 확인**할 것
- 향후 각 단계 완료 시마다 로컬 커밋만 하고 끝내지 말고, 배포 사이트로 테스트해야 하는 시점에는 반드시 push까지 됐는지 확인할 것 (v1 때는 매 단계 커밋+push했지만, v2 전환 이후 이 습관이 지켜지지 않음)

### 검증 상태
- 2차(사용자 실기기 확인): **아직 미완료** — push 직후라 Vercel 재배포 반영까지 1~2분 소요 예상

### 다음 작업 제안
1. 1~2분 후 https://memo-jade-mu.vercel.app 에서 강력 새로고침(Ctrl+Shift+R) 후 확인
   - 메모/캘린더/가계부 탭 전부 뜨는지 (v1이 아니라 v2 메모라이프 디자인인지)
   - 가계부 탭이 정상적으로 보이는지
2. 확인되면 → 10단계 "홈 대시보드 & 통합 검색" 진행. 이후 단계부터는 완료할 때마다 커밋+push까지 함께 할지 사용자와 확인

### 2차 확인 결과
- **완료** — 배포 사이트에서 v2(메모라이프) 정상 확인됨. 앞으로 매 단계마다 커밋+push 하기로 결정 (위 "4. 배포 규칙" 참고)

## 2026-07-27 — 추가 기능: 메모 공유하기

### 배경
- 브리핑/체크리스트에는 없었지만 사용자가 직접 요청 ("메모는 공유하기 기능도 추가해줘")

### 완료한 작업
- 메모 카드 액션 버튼에 "공유" 추가 (고정/공유/수정/삭제 순)
- `js/app.js`에 `shareMemo`/`copyToClipboard` 추가
  - `navigator.share` 지원 시 제목+내용으로 공유 시트 호출
  - 미지원이거나 호출 자체가 동기적으로 실패하는 경우, 공유가 reject되는 경우(사용자가 취소한 AbortError는 제외) 전부 클립보드 복사로 자동 대체 — v1에서 겪었던 "공유 버튼 무반응" 버그의 교훈을 반영해 처음부터 견고하게 작성
- `CLAUDE.md` 7단계에 "메모 공유하기" 항목 추가
- `service-worker.js` 캐시 버전 v1 → v2 (memolife-cache)
- 새 배포 규칙에 따라 커밋 + push 완료 (아래 참고)

### 검증 상태
- 1차(Claude 자동 확인): 함수 선언/참조 정적 검토 완료.
- 2차(사용자 실기기 확인): **완료** — 배포 사이트에서 공유 기능 정상 동작 확인됨

### 다음 작업 제안
- 10단계 "홈 대시보드 & 통합 검색" 진행

## 2026-07-27 — 10단계: 홈 대시보드 & 통합 검색

### 완료한 작업
- `index.html`: 상단바에 검색(🔍)/공유(↗) 버튼 추가, 검색 화면(`screen-search`) 신설, 홈 화면 전체 마크업으로 교체 (오늘 요약, 빠른 기록, 고정 카드 그리드 3종, 오늘의 타임라인, 이번 주 지출 비교, 1년 전 오늘, 데이터 백업 카드는 11단계 예정 안내만)
- `js/app.js`
  - `homeCount`, `renderHome` 추가 — 오늘 요약, 타임라인, 이번 주/지난 주 지출 비교 막대, 1년 전 오늘 회고 카드
  - 메모/일정/가계부 고정 카드 그리드 렌더링을 원본처럼 3번 복붙하지 않고 `renderPinGrid()` 공용 함수로 추출해서 재사용 (원본과의 의도적 차이 — 로직은 동일)
  - `[data-goto]` 빠른 기록 버튼 → 해당 탭 이동
  - 상단 ↗ 버튼: 오늘 요약을 `navigator.share`로 공유, 실패 시 클립보드 복사 (v1/메모 공유 버튼과 동일한 견고한 패턴)
  - 통합 검색: `renderSearch` — 메모/일정/가계부를 제목·내용·카테고리 기준으로 검색, 필터 칩(전체/메모/일정/가계부), 결과 클릭 시 해당 탭으로 이동
  - `renderAll()`에 `renderHome()`/`renderSearch()` 추가 — 이제 6개 화면(홈/메모/캘린더/가계부/검색) 전부 연결됨
- `service-worker.js` 캐시 버전 v2 → v3

### 검증 상태
- 1차(Claude 자동 확인): `getElementById` 참조 전수 대조(홈/검색 관련 id 포함, `renderPinGrid`은 변수로 id를 넘기는 방식이라 별도로 호출부 3곳의 boxId/labelId 문자열이 HTML id와 정확히 일치하는지 직접 대조함), 중괄호/소괄호/백틱 짝 확인 완료.
- 2차(사용자 실기기 확인): **아직 미완료**

### 다음 작업 제안
1. 배포(커밋+push 후, 아래 참고) 사이트에서 확인
   - 홈 화면: 오늘 요약, 빠른 기록 버튼(+메모/+일정/+지출)으로 각 탭 이동되는지
   - 메모/일정/가계부를 고정하면 홈 화면에 3열 카드로 뜨는지, 카드 클릭 시 해당 탭 이동, ✕로 고정 해제되는지
   - 오늘의 타임라인에 오늘 등록한 항목들이 다 보이는지
   - 이번 주 지출 비교 막대, (있다면) 1년 전 오늘 카드
   - 🔍 검색 버튼 → 검색 화면 이동, 검색어/필터 칩으로 결과 필터링되는지, 결과 클릭 시 탭 이동
   - ↗ 공유 버튼으로 오늘 요약 공유/복사되는지
2. 확인되면 → 11단계 "설정 & 백업" 진행 (홈 화면 "데이터 백업" 카드 실제 구현)

### 2차 확인 결과
- **완료** — 배포 사이트에서 홈/검색 기능 정상 확인됨

## 2026-07-27 — UI 정리: 탭바 가운데 + 버튼 삭제

### 배경
- 사용자 요청: "가운데 있는 + 기능은 아무런 기능이 없으니까 삭제해줘"
- 실제로는 메모 탭 이동 + 작성폼 자동 오픈에 연결돼 있었지만(완전한 무기능은 아니었음), "+ 메모" 퀵버튼·메모 탭 자체의 "+ 새 메모 추가" 버튼과 기능이 겹쳐 불필요하다고 판단하신 것으로 이해하고 삭제 진행

### 완료한 작업
- `index.html`: 탭바에서 가운데 `tab-add`(＋) 요소 제거 (홈/메모/캘린더/가계부 4개 탭만 남음, flex로 균등 배치)
- `js/app.js`: `tabAddBtn` 클릭 리스너 제거
- `css/style.css`: 사용하지 않게 된 `.tab-add` 스타일 규칙 제거
- `service-worker.js` 캐시 버전 v3 → v4

### 검증 상태
- 1차(Claude 자동 확인): `tabAddBtn`/`tab-add` 관련 참조가 세 파일 어디에도 안 남아있는지 grep으로 전수 확인.
- 2차(사용자 실기기 확인): **완료** — 탭바 4개만 정상적으로 보이고 전환도 잘 됨

### 다음 작업 제안
- 11단계 "설정 & 백업" 진행

## 2026-07-27 — 11단계: 설정 & 백업

### 완료한 작업
- `index.html`의 홈 화면 "데이터 백업" 안내 문구를 실제 내보내기/가져오기 버튼 + 숨겨진 파일 입력으로 교체
- `js/app.js`에 백업 로직 추가
  - 내보내기: `K`(전체 localStorage 키 목록)를 순회해서 하나의 JSON으로 묶어 `memolife-backup-YYYY-MM-DD.json` 파일로 다운로드
  - 가져오기: 파일 선택 → `FileReader`로 읽어 JSON 파싱 → 각 키별로 값이 있으면 localStorage에 복원 → `renderAll()`로 전체 화면 갱신, 형식이 잘못된 파일이면 "파일을 읽을 수 없어요" 안내
- `service-worker.js` 캐시 버전 v4 → v5
- 이것으로 CLAUDE.md 6~11단계(메모라이프 전체 개편 + 확장 기능) 전부 완료. 남은 건 12단계 "PWA 재검증 & 배포"뿐

### 검증 상태
- 1차(Claude 자동 확인): `getElementById` 참조 전수 대조, 중괄호/소괄호/백틱 짝 확인 완료.
- 2차(사용자 실기기 확인): **완료** — 내보내기/가져오기 정상 동작 확인됨

### 다음 작업 제안
- 12단계 "PWA 재검증 & 배포"로 마무리

## 2026-07-27 — 12단계: PWA 재검증 & 배포

### 완료한 작업
- `icons/icon-192x192.png`, `icons/icon-512x512.png`를 참고 zip에 들어있던 메모라이프 전용 아이콘(어두운 배경 + 파스텔 원 + "메" 글자)으로 교체 (기존 "MY MEMO" 회색 아이콘 대체)
- `manifest.json` 전면 갱신: name/short_name "메모라이프", description 갱신, background_color `#FAFAF9`, theme_color `#22262B`, `lang: "ko"` 추가 (아이콘 경로는 기존 파일명 그대로 유지해서 다른 참조는 안 건드림)
- `index.html`은 6단계에서 이미 title/description/theme-color가 메모라이프 기준으로 맞춰져 있어서 추가 수정 불필요 확인
- `service-worker.js`의 `CORE_ASSETS`는 새 파일 구조(단일 index.html/css/js/icons)와 이미 일치함을 확인 — 새로 추가된 파일 없음. 캐시 버전만 v5 → v6으로 갱신
- `CLAUDE.md` 기본 정보의 배포 URL 각주 정리 ("v2 배포 후 갱신 예정" → "v2 배포 완료")

### 검증 상태
- 1차(Claude 자동 확인): manifest.json JSON 구조 육안 검토, 아이콘 파일 교체 확인, CORE_ASSETS와 실제 파일 목록 대조 완료.
- 2차(사용자 실기기 확인): **완료** (iOS/사파리)
  - 처음에 "앱 설치가 안 보여요"라고 하셨는데, 확인해보니 iOS 사파리는 크롬처럼 브라우저 메뉴에 자동으로 "앱 설치" 항목이 뜨는 기능 자체가 없음(버그 아님, 플랫폼 특성). 공유 버튼 → "홈 화면에 추가"로 안내드림 → 정상 설치 확인됨
  - 오프라인 작동(비행기 모드)도 정상 확인됨

### 다음 작업 제안
- 없음. v2(메모라이프) 확장 프로젝트 6~12단계 전체 완료.

## 2026-07-27 — 앱 아이콘 재변경 (직접 생성)

### 배경
- 사용자 요청: "앱 아이콘 변경해줘" → 방법을 여쭤보니 "간단한 테스트/아이콘으로 제가(Claude가) 새로 만들어주기" 선택

### 완료한 작업
- 이미지 생성/편집 도구(Node, Python 등)가 없는 환경이라, **PowerShell + .NET(System.Drawing)**으로 직접 PNG 아이콘 생성
  - 배경: 브랜드 accent 색상(`#22262B`, manifest theme_color와 동일)
  - 중앙에 흰색 굵은 글씨로 "메" 한 글자 (maskable 안전영역을 고려해 여백 넉넉하게)
  - 192x192, 512x512 두 사이즈 각각 직접 렌더링 (확대/축소로 인한 화질 저하 없음)
- `icons/icon-192x192.png`, `icons/icon-512x512.png` 덮어쓰기 — 파일명 동일해서 `manifest.json`/`index.html` 추가 수정 불필요
- `service-worker.js` 캐시 버전 v6 → v7

### 검증 상태
- 1차(Claude 자동 확인): 생성된 두 이미지를 직접 열어서 육안 확인 완료 (다크 배경 + "메" 흰 글씨, 두 사이즈 모두 깨짐 없이 선명함)
- 2차(사용자 실기기 확인): **아직 미완료**

### 다음 작업 제안
1. 배포 사이트에서(Ctrl+Shift+R, 아이콘은 캐시가 오래 남을 수 있어 반영에 시간이 걸릴 수 있음) 새 아이콘이 보이는지 확인
   - 브라우저 탭 아이콘, iOS라면 "홈 화면에 추가"로 새로 설치해서 확인 (이미 설치돼 있다면 삭제 후 재설치해야 아이콘이 갱신될 수 있음)
2. 마음에 안 들면 색상/글자/스타일을 다시 요청해주세요

## 2026-07-27 — 앱 아이콘 재변경 (사용자 제공 이미지)

### 배경
- 사용자가 `C:\Users\Admin\Downloads\Untitled-1.png`(핑크색 메모지 스타일 "MEMO" 아이콘, 1024x1024) 제공하며 이걸로 교체 요청

### 완료한 작업
- PowerShell + System.Drawing으로 원본 이미지를 192x192, 512x512로 고품질 리사이즈(HighQualityBicubic)해서 `icons/icon-192x192.png`, `icons/icon-512x512.png`에 저장
- `service-worker.js` 캐시 버전 v7 → v8

### 검증 상태
- 1차(Claude 자동 확인): 리사이즈된 192px 이미지를 직접 열어서 원본 디자인이 깨지지 않고 잘 축소됐는지 육안 확인 완료
- 2차(사용자 실기기 확인): **아직 미완료**

### 다음 작업 제안
1. 배포 사이트에서(Ctrl+Shift+R) 새 아이콘 확인, 이미 설치돼 있다면 삭제 후 재설치 필요할 수 있음
2. 확인되면 이번 아이콘 관련 요청까지 전부 마무리

## 2026-07-27 — 버그 수정: 헤더가 상태바(시계)와 겹침

### 배경
- 사용자 리포트: 홈 화면에 설치한 상태(standalone)에서 상단 헤더가 아이폰 상태바 시계와 겹쳐 보임

### 완료한 작업
- `css/style.css`의 `.topbar`에 `padding-top: calc(env(safe-area-inset-top) + 37px)` 적용
  - 사용자가 요청한 "margin-bottom 15px"는 헤더 자체를 내리는 효과가 없어서(margin-bottom은 헤더 아래 요소를 밀어낼 뿐) `padding-top`으로 대체
  - 기종마다 다른 상태바/노치 높이에 안전하게 대응하기 위해 고정값 대신 `env(safe-area-inset-top)`(기기가 알려주는 안전영역) + 여유 15px(기존 22px + 15px) 사용
  - `index.html`에 이미 `viewport-fit=cover`가 설정돼 있어 안전영역 값이 정상적으로 계산됨을 확인
- `service-worker.js` 캐시 버전 v8 → v9

### 검증 상태
- 1차(Claude 자동 확인): CSS 문법 및 기존 viewport 설정과의 호환 확인.
- 2차(사용자 실기기 확인): **완료** — 헤더가 상태바와 안 겹치고 정상적으로 보임

### 다음 작업 제안
- 없음

## 2026-07-27 — v2(메모라이프) 프로젝트 완료

브리핑 원안(단순 메모 앱) → 캘린더/가계부까지 포함한 메모라이프로 확장하는 작업이 모두 끝남.
- 배포 URL: https://memo-jade-mu.vercel.app
- GitHub: https://github.com/skdus4872-pixel/memo
- 화면: 홈(대시보드) / 메모 / 캘린더 / 가계부 / 통합검색, 다크모드, PWA(오프라인+설치), 데이터 백업까지 전부 구현 및 실기기 검증 완료

## 2026-07-28 — 13단계: 시작화면(스플래시)

### 배경
- 사용자 요청: 앱 실행 전에 `C:\Users\Admin\Downloads\시작화면.png`(분홍 그라데이션 + 토끼 캐릭터 + "MEMO LIFE" 로고 이미지)를 1.5초간 먼저 보여줄 것

### 완료한 작업
- 이미지를 `icons/splash.png`로 복사
- `index.html`: `#frame` 최상단(상단바보다 앞)에 `#splashScreen` 오버레이 추가 (이미지 전체 표시)
- `css/style.css`: `.splash-screen`을 `.frame` 기준 `position:absolute; inset:0; z-index:9999`로 배치해 폰 프레임 영역 전체를 덮도록 하고, `object-fit:cover`로 이미지 표시. `.hide` 클래스로 0.4초 페이드아웃 트랜지션 처리
- `js/app.js`: `DOMContentLoaded` 시 1.5초 뒤 `.hide` 클래스 추가 → 0.4초 후 DOM에서 완전히 제거 (이후 사용자 요청으로 3초로 변경, `setTimeout` 값 1500 → 3000)
- `service-worker.js` 캐시 목록(`CORE_ASSETS`)에 `./icons/splash.png` 추가, 캐시 버전 v9 → v10 (오프라인에서도 스플래시 이미지가 보이도록)
- `CLAUDE.md`에 "13단계. 시작화면(스플래시)" 섹션 신설 및 체크 완료 처리

### 검증 상태
- 1차(Claude 자동 확인): 로컬에 Python/Node 등 서버 실행 환경이 없고 브라우저 자동화 도구도 연결되지 않아, 실제 렌더링 대신 코드 정적 검토로 대체함 — `index.html`의 `#splashScreen` id와 `js/app.js`의 `getElementById('splashScreen')` 참조 일치, `icons/splash.png` 파일 실제 생성 확인(248KB), CSS `.splash-screen`/`.hide` 셀렉터 및 `.frame`과의 `position:relative`+`overflow:hidden` 관계로 프레임 영역 안에 정확히 덮이는지 검토 완료.
- 2차(사용자 실기기 확인): **완료** — 노출 시간을 3초로 변경 후 배포 사이트에서 정상 동작 확인됨

### 완료 후 처리
- 커밋(`5d903f6`) + GitHub push 완료, Vercel 자동 재배포 반영 확인됨

### 다음 작업 제안
- 없음. 13단계(시작화면) 완료.

## 2026-07-28 — 앱 아이콘 재변경 (사용자 제공 이미지 2차)

### 배경
- 사용자가 `C:\Users\Admin\Downloads\메모라이프 아이콘.png`(191×191, 핑크 도트 상단바 + 메모지 스타일) 제공하며 이걸로 교체 요청

### 완료한 작업
- PowerShell + System.Drawing으로 원본을 192x192, 512x512로 고품질 리사이즈(HighQualityBicubic)해서 `icons/icon-192x192.png`, `icons/icon-512x512.png`에 저장
  - 원본이 191×191로 작아 512로 확대 시 화질 저하 우려가 있었으나, 육안 확인 결과 선/도트 경계가 선명하게 유지됨
- `service-worker.js` 캐시 버전 v10 → v11

### 검증 상태
- 1차(Claude 자동 확인): 리사이즈된 512px 이미지를 직접 열어서 원본 디자인이 깨지지 않고 잘 확대됐는지 육안 확인 완료
- 2차(사용자 실기기 확인): **완료** — 커밋(`936b729`) + push 후 배포 사이트에서 새 아이콘 정상 확인됨

### 다음 작업 제안
- 없음

## 2026-07-28 — 상단바 로고를 이미지로 교체

### 배경
- 사용자가 `C:\Users\Admin\Downloads\이름.png`(235×48, "MEMO LIFE" 손글씨 스타일, 검정+보라) 제공하며 상단바 왼쪽 텍스트 로고("메모라이프")를 이 이미지로 교체 요청

### 완료한 작업
- 이미지를 `icons/brand-logo.png`로 복사
- `index.html`: `<div class="brand">메모<span>라이프</span></div>` → `<img class="brand-logo" src="icons/brand-logo.png" alt="메모라이프">`
- `css/style.css`: 더 이상 쓰이지 않는 `.brand`/`.brand span` 텍스트 스타일 제거, `.brand-logo`(height 22px, width auto로 기존 텍스트 로고와 비슷한 높이 유지, 비율 그대로 확대/축소) 추가
- `service-worker.js` 캐시 목록에 `./icons/brand-logo.png` 추가, 캐시 버전 v11 → v12

### 검증 상태
- 1차(Claude 자동 확인): `.brand` 클래스가 프로젝트 내 다른 곳에서 쓰이지 않는지 grep으로 확인 후 안전하게 제거, 이미지 육안 확인 완료
- 2차(사용자 실기기 확인): **완료** — 커밋(`c0a2813`) + push 후 정상 확인됨

### 다음 작업 제안
- 없음

## 2026-07-28 — 시작화면(스플래시) 이미지 교체 + 노출 시간 2초로 변경

### 배경
- 사용자가 `C:\Users\Admin\Downloads\수정본.png`(기존 시작화면의 수정본, 토끼 캐릭터+"MEMO LIFE" 로고) 제공하며 이걸로 교체 + 노출 시간을 2초로 변경 요청

### 완료한 작업
- `icons/splash.png`를 새 이미지로 덮어쓰기
- `js/app.js`: 스플래시 노출 시간 `setTimeout` 값 3000 → 2000
- `service-worker.js` 캐시 버전 v12 → v13
- `CLAUDE.md` 13단계 체크리스트 항목 갱신(2초, 이미지 교체 반영)

### 검증 상태
- 1차(Claude 자동 확인): 새 이미지 파일 정상 복사 확인, `setTimeout` 값 변경 확인
- 2차(사용자 실기기 확인): **완료** — 커밋(`9a274e6`) + push 후 정상 확인됨

### 다음 작업 제안
- 없음

## 2026-07-28 — 하단 탭바 아이콘을 SVG로 교체

### 배경
- 사용자가 `C:\Users\Admin\Downloads\아이콘.jpg`(플랫/미니멀 단색 아이콘 세트 스타일 참고 이미지)를 제공하며 앱 메인 아이콘이 아니라 하단 탭바의 홈/메모/캘린더/가계부 이모지(🏠📝📅💰)를 이런 스타일로 바꿔달라고 요청
- PNG 이미지 대신, 크기 변화에 깨지지 않고 다크모드/선택 상태 색상(`--ink-soft`/`--ink`)에 자동으로 맞춰지는 인라인 SVG로 구현하기로 결정 (이미지라면 색상별로 여러 장 만들어야 하는 번거로움 회피)

### 완료한 작업
- `index.html`: 탭바 4개 항목의 이모지를 각각 `fill="currentColor"` 인라인 SVG로 교체 (홈=집 모양, 메모=문서 모양, 캘린더=달력 모양, 가계부=지갑 모양)
- `css/style.css`: 기존 이모지용 `.tab .ic { font-size: 17px; }`를 SVG 크기 지정(`.tab .ic svg { width:19px; height:19px; }`)으로 교체, flex 중앙 정렬 추가
- `service-worker.js` 캐시 버전 v13 → v14 (새 이미지 파일은 없지만 index.html/css 변경 반영을 위해 버전만 갱신)

### 검증 상태
- 1차(Claude 자동 확인): 코드 정적 검토만 진행(SVG 마크업 태그/속성 짝 확인, `.tab`/`.ic` 클래스 연결 확인). 로컬에 브라우저 자동화 도구가 없어 실제 렌더링 모양은 자동으로 확인하지 못함.
- 2차(사용자 실기기 확인): **완료** — 커밋(`b688351`) + push 후 정상 확인됨

### 다음 작업 제안
- 없음

## 2026-07-28 — 버그 수정: 스크롤 시 하단 탭바가 화면 밖으로 사라짐

### 배경
- 사용자 리포트: 화면을 스크롤(내리기)했을 때 하단 탭바(홈/메모/캘린더/가계부)가 고정되지 않고 같이 스크롤되어 사라짐

### 원인
- `.frame`이 `min-height`로만 설정돼 있어서, 화면 콘텐츠가 길어지면 프레임 자체 높이가 늘어남
- 이렇게 되면 원래 의도했던 "내부 `.screen`만 스크롤(`overflow-y:auto`)되고 탭바는 프레임 기준으로 고정" 구조가 깨지고, 페이지 전체(또는 프레임 전체)가 스크롤되면서 프레임 기준으로 위치가 잡혀있던 `.tabbar`도 같이 밀려 올라가 화면 밖으로 나가버림

### 완료한 작업
- `css/style.css`의 `.frame`: `min-height` → 고정 `height`로 변경 (데스크톱 미리보기 `874px`, 모바일은 `100vh` + `100dvh`(주소창 크기 변화에 안정적인 최신 뷰포트 단위, 순서상 뒤에 와서 지원 브라우저에서 우선 적용))
- `service-worker.js` 캐시 버전 v14 → v15

### 검증 상태
- 1차(Claude 자동 확인): CSS 문법 검토, `.screen`의 `flex:1`+`overflow-y:auto`가 이제 프레임의 고정 높이를 기준으로 정상적으로 내부 스크롤 영역을 형성하는 구조인지 검토 완료. 실제 렌더링/스크롤 동작은 브라우저 자동화 도구가 없어 자동으로 확인하지 못함.
- 2차(사용자 실기기 확인): **아직 미완료**

### 다음 작업 제안
1. 로컬 또는 배포 사이트에서 내용이 긴 화면(예: 가계부 전체 내역, 홈 화면)을 아래로 스크롤했을 때 하단 탭바가 화면에 계속 고정돼 보이는지 확인
2. 확인되면 커밋+push 진행
